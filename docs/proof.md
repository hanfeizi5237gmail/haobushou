# 宣传内容证据说明

本文件记录宣传表述的依据与适用范围。资料来自已有运行截图、任务记录及 README 已落地说明；本轮文字改写不构成新一轮产品验收。发生冲突时，以真实运行记录和本文件为准。

## 证据怎么读

「真实运行」说明某次操作实际发生，不证明所有来源或系统均可复现。「界面示例」用于展示交互，不能充当真实库存、用户评价或性能证明。「已记录能力」沿用原有产品事实，不表示本轮重跑了测试。「规划」只说明方向。

## 真实链接收录

[article-capture.png](../assets/screenshots/article-capture.png) 显示一条 `mp.weixin.qq.com` 公开链接、识别 1 条链接，以及「成功 1 · 失败 0」。对应文章为「二狗的旅游攻略skill是怎么来的？」，公众号为「二狗说AI」。

该截图支持「公开文章链接已进入本地档案处理链路」，不支持任意链接成功、全部历史覆盖、无限量或 100% 成功。收录成功计数不能代替资源离线归档状态。

## 本地档案与离线阅读

[article-archive.png](../assets/screenshots/article-archive.png) 展示上述文章的标题、公众号、来源、处理版本、任务记录及阅读/导出入口。图中资源状态明确为「等待离线归档」，待处理 3，因此不能把这张图描述为该文章和图片已经完整离线保存。

已有产品事实：文章与任务以本地 SQLite 和文件为存储依据，已归档内容支持重启后读取；可按公众号筛选和搜索文章标题。宣传中的「检索」限定于上述范围，不延伸为全文搜索、语义搜索或云端检索。

[offline-reader.png](../assets/screenshots/offline-reader.png) 为隔离演示数据，展示本地阅读交互。图中标明来源和资源未验证；文章、评论均不能作为真实内容库存或用户好评。离线能力只适用于已成功保存的正文和资源，不承诺任意文章图文完整保留。

## 多格式导出

[export.png](../assets/screenshots/export.png) 是导出界面示例，显示 HTML、Markdown、Word、PDF、EPUB、Excel、JSON 选项。菜单观察本身不等于逐项导出验收。

原有证据文件记录的产品能力覆盖 Markdown、HTML、JSON、Excel、DOCX、PDF 及批量任务路径。本轮保留这六种格式，其中正文出口为 HTML、Markdown、Word（DOCX）、PDF，文章信息可导出 Excel、JSON，不承诺表格中的每个字段均有数据。

EPUB 当前仅有菜单展示，缺少独立导出工件验收，故不列入已确认卖点；这不是移除产品功能。图片能否离线使用取决于本地资源和导出方式，批量条数取决于套餐权益。完整数据库备份为会员能力。

## 会员授权与套餐

[membership.png](../assets/screenshots/membership.png) 展示套餐、授权编号、到期时间和设备激活数量。截图来自本地隔离测试，既不是客户账号，也不是生产授权证明；图中设备 1/1 不代表所有套餐统一上限。

已有产品口径：邮箱验证码登录，人工确认收款后签发固定天数授权；服务端和桌面端 Main Process 校验权益。免费版保留登录、反馈、本地阅读和单篇基础导出，批量/筛选出口免费基线最多 1 篇；会员按权益开放批量导出、完整数据库备份、设备激活和标准支持。免费版为社区支持。

月方案为 9.9 元 / 30 天，年方案为 99 元 / 360 天，均为作者已确认价格。本轮主文与海报只展示套餐及期限，不改变价格。套餐天数由后台配置；90 天可作为后续配置选择，不能写成当前在售方案。

收款采用闲鱼或人工方式，没有自动支付、自动扣款或自动续费。授权到期或撤销后回落免费权益；退款是否影响权益，以实际授权状态为准，不能笼统写成任何退款都自动撤销授权。本页不新增退款条件、到账时限、恢复次数或服务响应时限承诺。

本地/隔离验证不能证明生产邮件和线上授权交付已经全面开放。会员服务入口为 [member.abssqr.com](https://member.abssqr.com)，访问一个网页也不能证明注册、邮件、订单全链路可用；获取方式以页面和人工确认结果为准。

## 真实任务快照

2026-09-02 的一次 WeRead 历史同步低频试点记录：

| 阶段 | 数量 |
| --- | ---: |
| 发现列表候选 | 98 |
| 去重后进入正文队列 | 94 |
| 已产生本地归档工件 | 54 |
| 正文失败 | 1 |
| 用户暂停后保留待处理 | 39 |

这些数字来自原证据文件所记录的同一任务、同一窗口；94 = 54 + 1 + 39。它们解释计数与任务状态，不构成普遍容量、速度、成功率或图文完整归档承诺。公开仓库保留的是快照说明，并未附带原始任务数据库，本轮未重新执行该历史任务。

任务暂停、恢复、取消、失败重试和重启恢复沿用原 README 的已落地描述。该单次快照不单独证明每一种恢复路径，本轮也没有重新验收这些路径。

## 平台与版本

现有截图/说明对应桌面端 0.1.0 证据快照，不应推断为在线最新版本。

- macOS Apple Silicon：已有本地运行的候选制品；正式签名、公证和公开分发状态没有新增验证记录，安装前需确认。
- Windows：已生成交叉构建候选；真实 Windows 安装、运行、签名、SmartScreen 和独立人工验收尚未完成，不标为正式支持。
- macOS Intel：当前证据不支持兼容承诺。

下载页面只应展示完成真实制品验证的平台。本轮不新增或发布安装包。

## 产品演进的层次

近期方向：更多内容来源的独立验证、同步稳定性和异常恢复、导出检索与资料整理体验；继续 Windows 安装和发布验证。依据为原路线图及作者本次明确要求，不代表新增功能已验收。

长期探索：团队协作、成员和席位、内容共享及更完整的知识工作流。需要需求评估，不承诺版本、日期或购买后必然获得。当前不提供文章正文云上传、在线阅读、云端检索或远程采集；自动续费和复杂企业认证不属于本期范围。

## 隐私与使用边界

文章正文、任务、SQLite、备份、导出文件保存在桌面端，不上传商业化后台；会员后台处理账号、订单、授权、设备和用户主动提交的反馈。获取来源内容、登录和授权会发生网络请求，不应宣传为完全离线运行或所有数据绝不离开本机。

截图和诊断附件上传前必须去除 Token、Cookie、手机号、本地绝对路径、文章正文等敏感信息。公开文章标题、来源链接用于核对运行证据，不代表获准转载正文或出售文章数据。

公开内容与已授权来源仍受版权、访问权限、频控、验证码、页面变化及网络条件约束。WeRead 为非官方低频试点（原记录称 B 级试点），不是官方长期接口；不能承诺任意公众号、完整历史、防封、无限同步或绕过限制。

## 维护规则

### 作者提供的联系入口

作者于 2026-09-06 提供 [闲鱼联系链接](https://m.tb.cn/h.8LRMVA7?tk=lMxVTgwIWSL) 和微信加好友二维码，明确用于读者咨询与会员开通。[仓库中的二维码](../assets/contact/wechat-qr.jpg) 由该图片裁切，保留完整码图和白边，去除无关头像、昵称及地区，未重绘二维码。原图没有可复制的文字微信号，宣传页不推断账号。

这些是作者提供的联系信息，不构成自动支付或即时授权承诺。本轮没有实际添加好友、验证闲鱼登录后跳转或进行交易。

### 新材料的证据要求

新增能力、数字、截图、平台或商业承诺，先补对应证据，再改宣传正文。原始截图保持不变；海报与视频注明真实运行、界面示例或隔离测试。不得把代码存在、按钮可见、网页可访问或本轮素材渲染成功，写成产品真实验收通过。

本轮编辑对照与事实账本见 [宣传材料复查结论](./publicity-review.md)。
